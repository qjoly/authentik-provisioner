authentik-provisioner has been installed as release {{ .Release.Name }}.

A Job ({{ include "authentik-provisioner.fullname" . }}) will provision authentik
{{- if .Values.job.helmHook }} on every install/upgrade (Helm hook){{- end }}.

Follow it with:

  kubectl -n {{ .Release.Namespace }} logs job/{{ include "authentik-provisioner.fullname" . }} -f

Provisioning target: {{ .Values.authentik.url }}
{{- if not .Values.authentik.token.value }}
API token: secret {{ .Values.authentik.token.existingSecret }} (key {{ .Values.authentik.token.key }})
{{- end }}

Edit the desired state under `.Values.config` (see config.example.yaml in the
repo for the full schema) and run `helm upgrade` to re-apply.
